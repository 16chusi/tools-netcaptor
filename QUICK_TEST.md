# 快速测试网络抓包功能

## 问题诊断

如果点击"加载"后看到白屏,可能的原因:

1. **网站禁止 iframe 嵌入** - 大多数网站(如 Google, Facebook)都禁止在 iframe 中加载
2. **跨域限制** - 无法注入监控脚本
3. **CSP 策略** - 内容安全策略阻止

## 解决方案

### 方案1: 使用内置测试服务器 (推荐)

应用启动后会自动在后台运行测试服务器 `http://localhost:9999`

**步骤:**
1. 启动应用: `wails dev`
2. 切换到"网络抓包工具"标签页
3. 输入: `http://localhost:9999`
4. 点击"加载"
5. 在加载的页面中点击测试按钮
6. 在右侧面板查看捕获的请求

### 方案2: 使用本地 HTML 文件

1. 打开 `test_network.html` 文件
2. 获取完整路径,例如: `/home/user/project/test_network.html`
3. 在网络抓包工具中输入: `file:///home/user/project/test_network.html`
4. 点击"加载"

### 方案3: 使用代理模式 (适用于所有网站)

**步骤:**
1. 点击"启动代理"按钮
2. 代理将在 `http://127.0.0.1:8888` 启动
3. 配置系统代理或浏览器代理:
   ```bash
   # Linux/Mac
   export http_proxy=http://127.0.0.1:8888
   export https_proxy=http://127.0.0.1:8888
   ```
4. 使用外部浏览器访问任意网站
5. 在应用中查看捕获的请求

## 调试技巧

### 查看控制台日志

在 Wails 应用中按 `F12` 打开开发者工具,查看:
- 是否有 JavaScript 错误
- iframe 是否加载成功
- 注入脚本是否执行

### 检查状态栏

应用会在状态栏显示:
- ✅ 页面加载成功 - 脚本注入成功
- ❌ 注入失败 - 跨域或其他错误
- iframe contentWindow 不可访问 - 跨域限制

### 测试可用的网站

以下网站通常允许 iframe 嵌入:
- `http://example.com` - 简单示例网站
- `http://localhost:9999` - 内置测试服务器
- `http://httpbin.org` - HTTP 测试服务

以下网站通常禁止 iframe:
- `https://google.com` - X-Frame-Options: DENY
- `https://facebook.com` - CSP 限制
- `https://github.com` - X-Frame-Options: DENY

## 验证功能

### 1. 前端拦截模式

```
输入: http://localhost:9999
点击: 加载
结果: 应该看到测试页面
操作: 点击页面中的测试按钮
预期: 右侧面板显示捕获的请求
```

### 2. 代理模式

```
点击: 启动代理
状态: 显示 "🟢 代理运行中: http://127.0.0.1:8888"
配置: 设置浏览器代理
访问: 任意网站
预期: 应用中显示所有网络请求
```

## 常见问题

**Q: 为什么 iframe 是白屏?**

A: 检查浏览器控制台,可能是:
- 网站返回 X-Frame-Options 头
- CSP 策略阻止
- 网络错误

**Q: 为什么看不到请求?**

A: 可能原因:
- 脚本注入失败(跨域)
- 页面没有发送请求
- 刷新间隔问题(等待1秒)

**Q: 如何测试 HTTPS 网站?**

A: 使用代理模式,但当前版本对 HTTPS 支持有限,建议:
- 测试 HTTP 网站
- 使用本地测试服务器
- 等待 HTTPS 代理完整实现

## 下一步

如果基本功能正常,可以:
1. 添加请求过滤功能
2. 实现请求重放
3. 完善 HTTPS 代理
4. 导出 HAR 格式
5. 添加性能分析

## 反馈

遇到问题请提供:
- 输入的 URL
- 控制台错误信息
- 状态栏显示的消息
- 操作系统和浏览器版本
