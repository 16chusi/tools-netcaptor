# NetCaptor 配置文件

[log]
# 日志级别: DEBUG, INFO, WARNING, ERROR
level = DEBUG

# 日志输出方式: file(文件), console(控制台), both(两者)
mode = both

# 日志文件路径
file = logs/netcaptor.log

[window]
# 窗口默认宽度
width = 1400

# 窗口默认高度
height = 900

[proxy]
# 代理服务器端口
port = 8080
