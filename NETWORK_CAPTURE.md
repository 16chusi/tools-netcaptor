# 网络抓包工具使用说明

## 功能概述

网络抓包工具提供类似 Chrome DevTools 的网络监控功能,可以捕获网页中的所有 HTTP/HTTPS 请求和响应。

## 两种工作模式

### 1. 前端拦截模式 (默认)

**工作原理:**
- 在 iframe 中加载目标网页
- 注入 JavaScript 代码拦截 `fetch` 和 `XMLHttpRequest`
- 实时记录请求和响应数据

**优点:**
- 轻量级,无需额外配置
- 完全原生,网站无法检测
- 可以捕获 XHR、Fetch、WebSocket

**限制:**
- 跨域页面无法注入脚本
- 无法捕获静态资源(图片、CSS等)的原始请求
- 只能捕获 JavaScript 发起的请求

**使用方法:**
1. 在"网络抓包工具"标签页输入目标网址
2. 点击"加载"按钮
3. 在右侧面板查看捕获的请求和响应
4. 点击具体项目查看详细信息

### 2. 代理模式 (完整捕获)

**工作原理:**
- 启动本地 HTTP/HTTPS 代理服务器(默认端口 8888)
- 所有流量通过代理转发
- 在代理层面记录所有网络请求

**优点:**
- 可以捕获所有类型的请求(包括静态资源)
- 不受跨域限制
- 完全透明,网站无法检测

**限制:**
- 需要配置浏览器代理
- HTTPS 需要信任自签名证书(当前版本未实现)

**使用方法:**
1. 点击"启动代理"按钮
2. 代理服务器将在 `http://127.0.0.1:8888` 启动
3. 配置系统或浏览器使用该代理
4. 访问目标网站,所有请求将被自动捕获

## 界面说明

### 工具栏
- **URL 输入框**: 输入要监控的网页地址
- **加载**: 在 iframe 中加载目标网页
- **清空**: 清除所有已捕获的记录
- **启动/停止代理**: 控制代理服务器
- **导出**: 导出捕获数据为 JSON 格式

### 左侧 - 网页预览区
显示目标网页的实时内容

### 右侧 - 捕获面板

#### 请求标签页
显示所有捕获的 HTTP 请求:
- **Method**: 请求方法(GET/POST/PUT/DELETE)
- **URL**: 请求地址
- **Type**: 请求类型(xhr/fetch/proxy)

#### 响应标签页
显示所有捕获的 HTTP 响应:
- **Status**: HTTP 状态码
- **URL**: 响应地址
- **Size**: 响应体大小
- **Duration**: 请求耗时(毫秒)

#### 详情面板
点击任意请求或响应,在底部查看完整的 JSON 数据,包括:
- Headers(请求/响应头)
- Body(请求/响应体)
- 时间戳等元数据

## 数据导出

点击"导出"按钮可以将所有捕获的数据导出为 JSON 文件,格式如下:

\`\`\`json
{
  "requests": [
    {
      "id": "req_xxx",
      "url": "https://example.com/api/data",
      "method": "GET",
      "headers": {...},
      "body": "",
      "type": "fetch",
      "time": 1234567890
    }
  ],
  "responses": [
    {
      "id": "req_xxx",
      "url": "https://example.com/api/data",
      "status": 200,
      "headers": {...},
      "body": "{...}",
      "size": 1024,
      "duration": 150
    }
  ]
}
\`\`\`

## 技术实现

### 前端拦截
通过重写 `window.fetch` 和 `XMLHttpRequest.prototype` 实现请求拦截:
- 在请求发送前记录请求信息
- 在响应返回后记录响应信息
- 通过 Wails 绑定将数据传递到 Go 后端

### 代理服务器
使用 Go 标准库实现 HTTP 代理:
- 监听本地端口接收请求
- 转发请求到目标服务器
- 记录请求和响应数据
- 支持 HTTP CONNECT 隧道(HTTPS)

## 注意事项

1. **跨域限制**: 前端拦截模式下,跨域页面无法注入脚本,建议使用代理模式
2. **性能影响**: 捕获大量请求可能影响性能,建议定期清空记录
3. **数据大小**: 响应体默认只保存前 5KB,避免内存占用过大
4. **HTTPS 支持**: 代理模式的 HTTPS 支持需要生成和信任自签名证书(待实现)

## 常见问题

**Q: 为什么看不到任何请求?**
A: 可能是跨域限制,尝试使用代理模式或访问同源页面

**Q: 如何捕获图片、CSS 等静态资源?**
A: 使用代理模式可以捕获所有类型的请求

**Q: 导出的数据可以导入吗?**
A: 当前版本不支持导入,可以使用 JSON 工具查看和分析

**Q: 代理模式下 HTTPS 网站无法访问?**
A: 需要信任自签名证书,当前版本暂未实现完整的 HTTPS 代理

## 未来改进

- [ ] 完整的 HTTPS 代理支持
- [ ] HAR 格式导出
- [ ] 请求过滤和搜索
- [ ] 请求重放功能
- [ ] WebSocket 监控
- [ ] 性能分析(瀑布图)
